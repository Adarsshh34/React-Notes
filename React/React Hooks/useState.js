useState is a React Hook used to add state to functional components.
  
const [state, setState] = useState(initialValue);
    state → current value
    setState → function to update it
    initialValue → value used on first render only
Updating state causes re-render
✔ UI reflects new state automatically
